# Env de produção — NÃO commitar com valores reais
# Preencher no painel do Render como variáveis de ambiente

PORT=3001
NODE_ENV=production

# Sem banco de dados externo necessário — backend usa memória in-memory
# Adicione se quiser persistência futura:
# DATABASE_URL=

# Segredo JWT (não necessário atualmente, mas reservado)
# JWT_SECRET=
